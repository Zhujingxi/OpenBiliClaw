# OpenBiliClaw backend via the prebuilt multi-arch image on GHCR.
#
# This two-file distribution needs no git clone or local build:
#
#   mkdir -p litellm
#   curl -fsSLO https://raw.githubusercontent.com/whiteguo233/OpenBiliClaw/main/docker-compose.prebuilt.yml
#   curl -fsSL https://raw.githubusercontent.com/whiteguo233/OpenBiliClaw/main/litellm/config.yaml -o litellm/config.yaml
#   # BEGIN private .env bootstrap
#   python3 - <<'PY'
#   import base64, fcntl, hashlib, json, os, secrets, stat, subprocess
#   from pathlib import Path
#   #
#   env_path = Path('.env')
#   lock_path = Path('.env.lock')
#   if lock_path.is_symlink():
#       raise SystemExit('refusing symlinked .env lock')
#   lock_fd = os.open(lock_path, os.O_RDWR | os.O_CREAT | getattr(os, 'O_NOFOLLOW', 0), 0o600)
#   stage_path = Path(f'.env.stage-{os.getpid()}-{secrets.token_hex(4)}')
#   #
#   def read_private(path):
#       try:
#           before = path.lstat()
#       except FileNotFoundError:
#           return [], {}
#       if stat.S_ISLNK(before.st_mode) or not stat.S_ISREG(before.st_mode):
#           raise SystemExit('.env must be a private regular file')
#       fd = os.open(path, os.O_RDONLY | getattr(os, 'O_NOFOLLOW', 0))
#       try:
#           current = os.fstat(fd)
#           if (before.st_dev, before.st_ino) != (current.st_dev, current.st_ino):
#               raise SystemExit('.env changed during secure read')
#           if current.st_uid != os.getuid() or stat.S_IMODE(current.st_mode) != 0o600:
#               raise SystemExit('.env must be owned by this user with mode 0600')
#           with os.fdopen(fd, encoding='utf-8') as stream:
#               fd = -1
#               lines = stream.read().splitlines()
#       finally:
#           if fd >= 0:
#               os.close(fd)
#       values = dict(line.split('=', 1) for line in lines if line and not line.startswith('#') and '=' in line)
#       return lines, values
#   #
#   def render(lines, values):
#       output, emitted = [], set()
#       for line in lines:
#           key = line.split('=', 1)[0].strip() if '=' in line and not line.lstrip().startswith('#') else ''
#           if key in values:
#               output.append(f'{key}={values[key]}'); emitted.add(key)
#           else:
#               output.append(line)
#       output.extend(f'{key}={value}' for key, value in values.items() if key not in emitted)
#       return '\n'.join(output).rstrip('\n') + '\n'
#   #
#   def atomic_private(path, content):
#       temporary = path.with_name(f'{path.name}.tmp-{os.getpid()}-{secrets.token_hex(4)}')
#       fd = os.open(temporary, os.O_WRONLY | os.O_CREAT | os.O_EXCL, 0o600)
#       try:
#           with os.fdopen(fd, 'w', encoding='utf-8') as stream:
#               fd = -1; stream.write(content); stream.flush(); os.fsync(stream.fileno())
#           os.replace(temporary, path); os.chmod(path, 0o600)
#           parent = os.open('.', os.O_RDONLY)
#           try: os.fsync(parent)
#           finally: os.close(parent)
#       finally:
#           if fd >= 0: os.close(fd)
#           temporary.unlink(missing_ok=True)
#   #
#   try:
#       if not stat.S_ISREG(os.fstat(lock_fd).st_mode):
#           raise SystemExit('.env lock must be a regular file')
#       os.fchmod(lock_fd, 0o600); fcntl.flock(lock_fd, fcntl.LOCK_EX)
#       lines, existing = read_private(env_path)
#       base = dict(existing)
#       for name, prefix in (
#           ('LITELLM_POSTGRES_PASSWORD', ''), ('LITELLM_MASTER_KEY', 'sk-'),
#           ('OPENBILICLAW_SECRET_KEY', ''), ('OPENBILICLAW_ACCESS_TOKEN', ''),
#           ('OPENBILICLAW_SESSION_SECRET', ''),
#       ):
#           if not base.get(name, '').strip(): base[name] = prefix + secrets.token_hex(32)
#       base.setdefault('OPENBILICLAW_API_HOST', os.getenv('OPENBILICLAW_API_HOST', '0.0.0.0'))
#       base.setdefault('OPENBILICLAW_API_PORT', os.getenv('OPENBILICLAW_API_PORT', '8420'))
#       base.setdefault('OPENBILICLAW_LITELLM_ADMIN_URL', os.getenv('OPENBILICLAW_LITELLM_ADMIN_URL', f"http://127.0.0.1:{os.getenv('LITELLM_PORT', '4000')}/ui"))
#       atomic_private(env_path, render(lines, base))
#       staged, created = dict(base), {}
#       rotate = os.getenv('ROTATE_ACCESS') == '1'
#       if rotate or not staged.get('OPENBILICLAW_WEB_PASSWORD_HASH', '').strip():
#           password, salt = secrets.token_urlsafe(18), secrets.token_bytes(16)
#           derived = hashlib.scrypt(password.encode(), salt=salt, n=2**14, r=8, p=1, dklen=32, maxmem=64 * 1024 * 1024)
#           encode = lambda value: base64.urlsafe_b64encode(value).rstrip(b'=').decode('ascii')
#           staged['OPENBILICLAW_WEB_PASSWORD_HASH'] = f'scrypt${2**14}$8$1${encode(salt)}${encode(derived)}'
#           created['web_password'] = password
#       if rotate or not staged.get('OPENBILICLAW_EXTENSION_ACCESS_KEYS', '').strip():
#           key_id, secret = secrets.token_hex(6), secrets.token_urlsafe(32)
#           created['extension_access_key'] = f'obc_ext_{key_id}.{secret}'
#           staged['OPENBILICLAW_EXTENSION_ACCESS_KEYS'] = json.dumps([f'{key_id}:{hashlib.sha256(secret.encode()).hexdigest()}'], separators=(',', ':'))
#       atomic_private(stage_path, render(lines, staged))
#       compose = ['docker', 'compose', '--env-file', str(stage_path), '-f', 'docker-compose.prebuilt.yml']
#       subprocess.run([*compose, 'config'], check=True, stdout=subprocess.DEVNULL)
#       subprocess.run([*compose, 'up', '-d', '--wait', '--wait-timeout', '120'], check=True)
#       atomic_private(env_path, render(lines, staged))
#       if created:
#           print('BOOTSTRAP_STATUS:' + json.dumps({'status': 'credentials', 'message': 'first_run_access', 'details': created}, sort_keys=True))
#   finally:
#       stage_path.unlink(missing_ok=True)
#       os.close(lock_fd)
#   PY
#   # END private .env bootstrap
#   # configure obc-interactive, obc-analysis, and obc-embedding at
#   # http://127.0.0.1:4000/ui
#   # then configure sources through /api/v1/onboarding. The packaged static
#   # Web and extension clients consume the generated vNext API contract.
#
# Upgrade to the newest release:
#
#   docker compose -f docker-compose.prebuilt.yml pull
#   docker compose -f docker-compose.prebuilt.yml up -d
#
# To build from source instead (customization / development), use the
# docker-compose.yml at the repository root — see docs/docker-deployment.md.

services:
  litellm-postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: litellm
      POSTGRES_USER: litellm
      POSTGRES_PASSWORD: ${LITELLM_POSTGRES_PASSWORD:?Set LITELLM_POSTGRES_PASSWORD to a generated hex secret}
    volumes:
      - openbiliclaw_litellm_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U litellm -d litellm"]
      interval: 5s
      timeout: 5s
      retries: 12
    restart: unless-stopped

  litellm:
    # The official image can boot from DATABASE_URL/LITELLM_MASTER_KEY alone;
    # STORE_MODEL_IN_DB makes provider setup persistent through /ui.
    image: ghcr.io/berriai/litellm:v1.92.0
    depends_on:
      litellm-postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://litellm:${LITELLM_POSTGRES_PASSWORD:?Set LITELLM_POSTGRES_PASSWORD to a generated hex secret}@litellm-postgres:5432/litellm
      LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY:?Set LITELLM_MASTER_KEY to a generated sk-prefixed secret}
      STORE_MODEL_IN_DB: "True"
    command: ["--config=/app/config.yaml"]
    volumes:
      - ./litellm/config.yaml:/app/config.yaml:ro
    ports:
      - "127.0.0.1:${LITELLM_PORT:-4000}:4000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:4000/health/readiness', timeout=4)"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 40s
    restart: unless-stopped

  migrate:
    image: ghcr.io/whiteguo233/openbiliclaw-backend:latest
    environment:
      OPENBILICLAW_DATABASE_URL: sqlite:////app/runtime/data/vnext/openbiliclaw.db
    command: ["openbiliclaw", "db", "migrate"]
    volumes:
      - openbiliclaw_data:/app/runtime/data
    restart: "no"

  api:
    # Multi-arch image (linux/amd64 + linux/arm64) published by
    # .github/workflows/release-docker.yml on every backend release.
    # Pin a specific version by replacing `latest` with the desired release.
    image: ghcr.io/whiteguo233/openbiliclaw-backend:latest
    depends_on:
      migrate:
        condition: service_completed_successfully
      litellm:
        condition: service_healthy
    environment:
      OPENBILICLAW_PROJECT_ROOT: /app/runtime
      OPENBILICLAW_LITELLM_BASE_URL: http://litellm:4000
      OPENBILICLAW_LITELLM_API_KEY: ${LITELLM_MASTER_KEY:?Set LITELLM_MASTER_KEY to a generated sk-prefixed secret}
      OPENBILICLAW_LITELLM_ADMIN_URL: ${OPENBILICLAW_LITELLM_ADMIN_URL:-http://127.0.0.1:${LITELLM_PORT:-4000}/ui}
      OPENBILICLAW_DATABASE_URL: sqlite:////app/runtime/data/vnext/openbiliclaw.db
      OPENBILICLAW_HUEY_PATH: /app/runtime/data/vnext/huey.db
      OPENBILICLAW_SECRET_KEY: ${OPENBILICLAW_SECRET_KEY:?Set OPENBILICLAW_SECRET_KEY to a generated secret}
      OPENBILICLAW_ACCESS_TOKEN: ${OPENBILICLAW_ACCESS_TOKEN:?Set OPENBILICLAW_ACCESS_TOKEN to a generated secret}
      OPENBILICLAW_WEB_PASSWORD_HASH: ${OPENBILICLAW_WEB_PASSWORD_HASH:?Set OPENBILICLAW_WEB_PASSWORD_HASH to the installer-generated scrypt hash}
      OPENBILICLAW_SESSION_SECRET: ${OPENBILICLAW_SESSION_SECRET:?Set OPENBILICLAW_SESSION_SECRET to a generated secret}
      OPENBILICLAW_EXTENSION_ACCESS_KEYS: ${OPENBILICLAW_EXTENSION_ACCESS_KEYS:?Set OPENBILICLAW_EXTENSION_ACCESS_KEYS to installer-generated digest records}
      OPENBILICLAW_API_PORT: ${OPENBILICLAW_API_PORT:-8420}
    command: ["openbiliclaw", "serve", "--host", "0.0.0.0", "--port", "${OPENBILICLAW_API_PORT:-8420}"]
    # ``host.docker.internal`` resolves an explicitly configured host proxy.
    # Required on Linux Docker; auto-provided by Docker Desktop on Mac/Windows.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${OPENBILICLAW_API_HOST:-0.0.0.0}:${OPENBILICLAW_API_PORT:-8420}:${OPENBILICLAW_API_PORT:-8420}"
    volumes:
      - openbiliclaw_data:/app/runtime/data
      - openbiliclaw_logs:/app/runtime/logs
    healthcheck:
      test: ["CMD", "python", "-c", "import os,urllib.request,sys; port=os.getenv('OPENBILICLAW_API_PORT','8420'); sys.exit(0 if urllib.request.urlopen(f'http://127.0.0.1:{port}/api/v1/system/readiness', timeout=4).status==200 else 1)"]
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 3
    restart: unless-stopped

  worker:
    image: ghcr.io/whiteguo233/openbiliclaw-backend:latest
    depends_on:
      migrate:
        condition: service_completed_successfully
      litellm:
        condition: service_healthy
    environment:
      OPENBILICLAW_DATABASE_URL: sqlite:////app/runtime/data/vnext/openbiliclaw.db
      OPENBILICLAW_HUEY_PATH: /app/runtime/data/vnext/huey.db
      OPENBILICLAW_LITELLM_BASE_URL: http://litellm:4000
      OPENBILICLAW_LITELLM_API_KEY: ${LITELLM_MASTER_KEY:?Set LITELLM_MASTER_KEY to a generated sk-prefixed secret}
      OPENBILICLAW_SECRET_KEY: ${OPENBILICLAW_SECRET_KEY:?Set OPENBILICLAW_SECRET_KEY to a generated secret}
      OPENBILICLAW_WORKERS: "4"
      OPENBILICLAW_WORKER_RUNTIME_FACTORY: openbiliclaw.infrastructure.jobs.worker:database_runtime_factory
    command: ["openbiliclaw", "worker", "--workers", "4"]
    volumes:
      - openbiliclaw_data:/app/runtime/data
    # Running PID 1 is necessary but insufficient: the probe also requires the
    # application schema at Alembic head and an integrity-readable Huey queue.
    healthcheck:
      test: ["CMD", "python", "-m", "openbiliclaw.infrastructure.jobs.health"]
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 3
    restart: unless-stopped

volumes:
  openbiliclaw_data:
  openbiliclaw_logs:
  openbiliclaw_litellm_postgres:
