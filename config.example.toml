# OpenBiliClaw current configuration. Secret values live in the credential vault;
# this file contains opaque references only.

[model]
provider = "ollama"
model_name = ""
# credential_ref = "keyring:model-primary"

[content]
enabled = ["bilibili", "youtube", "bangumi", "v2ex", "reddit", "x", "zhihu", "linuxdo", "weibo", "rednote", "douyin"]

[recommendation]
pool_target_count = 100

[host]
api_host = "127.0.0.1"
api_port = 8420

[runtime]
default_timeout_seconds = 30.0
default_resource_limit = 4
