// Placeholder so `tsc -p tsconfig.popup.json` has at least one input before
// the popup migration (Phase E) lands its first popup/*.ts file.
// Remove this file once popup sources exist.
export {};
